import * as discord from 'discord.js'
exports = {
    execute : (commandName: string, client: discord.Client, msg:discord.Message, args:string[]) => {
        const cmd = require(`../comandos/${commandName}`)
        
        try{
            cmd.run(client, msg, args)
        }catch(err){
            return
        }
    }
}